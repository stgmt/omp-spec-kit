---
description: Inventory direct .specs children in the active project without modifying them.
---

Call the `spec_inventory` tool for the active project. Summarize its entries in lexical order and report every returned diagnostic and the `truncated` flag. Do not infer specification readiness or duplicate the inventory with direct filesystem operations.
