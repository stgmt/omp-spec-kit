# OMP Review Kit commit review

- staged diff hash: a136018120d31443be49dae3799880b93bd3c821b05b5cf8d1eb13f33fb150a8
- result: PASS
- reviewer models tried: @smol

### Review coverage

- **Staged diff hash**: `a136018120d31443be49dae3799880b93bd3c821b05b5cf8d1eb13f33fb150a8`
- **Changed paths**:
  - `package.json`
  - `src/service/git.js`
  - `src/service/http.js`
  - `src/service/index.js`
  - `src/service/onboarding.js`
  - `tests/e2e/lib/live-fixture.mjs`
  - `tests/helpers/git-env.mjs`
  - `tests/helpers/isolate-git-env.mjs`
  - `tests/service/onboarding.test.mjs`
  - `tests/unit/service/git-auth.test.mjs`
  - `tests/unit/service/onboarding.test.mjs`
- **Active review skills**: `skill://reality-first-review`, `skill://multi-stage-review`.
- **Stages executed**:
  - Stage 1: Context Scout (`review-context-scout`) — mapped change purpose, blast radius, callers across `mounts.js`, `writepath.js`, `sync.js`, `drift.js`, and invariants in onboarding and git execution.
  - Stage 2: Parallel Risk Hunting (`review-risk-hunter`) — evaluated correctness and security lanes against boundary conditions, token revocation, scope resolution, credential logging, SSRF, Git environment leaks, and the anti-parasitic architecture gate. Produced 0 defect candidates.
  - Stage 3: Adversarial Verification (`review-finding-verifier`) — verified absence of defects and confirmed robust defenses across all touched paths.
  - Stage 4: Orchestrator Synthesis (`reviewer-kit`) — consolidated findings and determined pass status.

### Confirmed findings

None.

### Unproven/rejected summary

No defect candidates were raised by the risk hunting lanes. All probed boundary conditions and failure modes are defended by existing contracts and automated tests.

### Verified-OK

- **`src/service/onboarding.js`**:
  - Identity binding: Minting requests strictly use verified `ctx.identity.login` from `authGate`; unauthenticated access fails closed with `401 UNAUTHENTICATED`.
  - User verification: Validates caller existence (`/hub/api/rest/users?query=login:...`) and ban status (`user.banned !== true`), returning `404 UNKNOWN_USER` or `403 BANNED`.
  - Token lifecycle: Existing permanent tokens named `omp-spec-kit agent` are revoked before minting a new one, preventing orphaned token accumulation.
  - Upstream scoping: Dynamically discovers Hub services (`GET /hub/api/rest/services?fields=id,name`) and attaches every resolved service ID to the minted token's scope.
  - Resiliency: Hub HTTP calls enforce a 15-second `AbortSignal` timeout; 5xx errors map to retryable `503 UNAVAILABLE`, and other failures map to `502 UPSTREAM_ERROR`.
  - Secret containment: The minted permanent token is never written to SQLite (`registry.db`) or service logs; audit logs record only `op: 'onboarding'` metadata with no secret leakage.
- **`src/service/http.js`**:
  - Route exposure: `POST /onboarding/token` correctly mounted behind `authGate(db, youtrackClient)`.
  - Input handling: Uses incoming request origin or configured `serviceUrl` for MCP config snippet generation without SSRF sinks.
- **`src/service/git.js`**:
  - CWD requirement: `GitClient.run` strictly mandates a non-empty string `cwd`, rejecting with `GitError` if omitted or empty. Verified that all repository callers (`mounts.js`, `writepath.js`, `sync.js`, `drift.js`, `registry.js`) supply valid directory paths.
  - Environment sanitization: Strips `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, and `GIT_ALTERNATE_OBJECT_DIRECTORIES` in `childEnv()`, isolating spawned git processes from host and repo git contexts.
  - Credential redaction: Sanitizes Authorization tokens and bearer credentials from stderr/error messages.
- **Test coverage**:
  - `tests/unit/service/onboarding.test.mjs`: Unit tests for Hub REST client mocks, revocation, error mapping, and audit logging.
  - `tests/unit/service/git-auth.test.mjs`: Unit tests for environment isolation, cwd enforcement, and token redaction.
  - `tests/service/onboarding.test.mjs`: E2E integration tests validating end-to-end token issuance, audit log persistence, anonymous rejection, and subsequent MCP `spec_patch` authentication.
  - `tests/helpers/isolate-git-env.mjs`: Ensures test runner subprocesses run with sanitized git environments.

REVIEW_RESULT=PASS

Working...
