# OMP Review Kit commit review

- staged diff hash: 77d43bdc5e71072abeb7ac8170c50483a58691ee062459db72430e4922765618
- result: PASS
- reviewer models tried: devin/swe-2

### Review coverage

Staged change (hash 77d43bdc…6158, 34 files, +1221/−27): implements the first-write elicitation guard — the MCP core refuses the first dryRun:false apply that creates a missing canonical spec Markdown document (REFUSED / ELICITATION_REQUIRED / skill://spec-elicitation), records a per-spec+document ticket, and allows the retry. New src/authoring/elicitation-guard.js (FirstWriteElicitationGuard), beforeMissing plumbing in proposals.js, guard invocation inside withWriteLock in service.js, ELICITATION_HINT in MCP_SERVER_INSTRUCTIONS, new plugins/omp-spec-kit/skills/spec-elicitation/SKILL.md, byte-identical dist mirrors with verified manifest.json hashes, the .specs/agent-ux-elicitation-guard/ corpus (15 docs), and test wiring (test:unit script, verify.yml step, new node:test edge suite, 4 BDD scenarios, staged-mcp archive scenario updated to REFUSED-then-retry).

Stages executed: context scout → parallel correctness+security risk hunters → adversarial verifier → local synthesis. Skills used: reality-first-review, multi-stage-review; domain contract from the staged spec corpus.

Verified-healthy: refusal leaks nothing (lock released in finally, staging never created, applied map untouched); tickets-before-commit and different-payload retry per FR-4; beforeMissing server-computed and unspoofable; envelope extra fields pass the unconstrained data/error schema; preview never consumes a ticket; refusal atomic (zero bytes); guard key collision-free; spec_patch sole mutation surface; new unit suite 12/12 pass; check-spec-corpus clean.

### Confirmed findings

None.

### Unproven/rejected summary

correctness-1 (rejected) — 'Guard trusts compile-time existence; preimage check cannot distinguish missing from empty file' (src/authoring/service.js:244-245). Rejected on three grounds: (1) upstream defense — inside withWriteLock, refreshGraph re-scans the repo and refuses with retryable CONFLICT when the fingerprint differs from the proposal base snapshot (service.js:228-233); the fingerprint hashes a path:sha256 row per canonical document (kernel/graph/build.js:511-516, kernel/adapters/fs.js:82-140), so any create/delete in the compile→lock window is caught before the guard — direction (a) yields CONFLICT not spurious refusal, direction (b) yields CONFLICT before commitDocuments recreates anything. (2) contract — DESIGN.md defines candidacy on the compiled change via beforeMissing; NFR-DETERMINISM-1 binds the verdict to the sampled missing status; no apply-time re-check mandated. (3) diff ownership — the sha256(empty) missing-as-empty encoding is pre-existing unchanged context (service.js:236-241, proposals.js:235-236).

Security lane: zero candidates — guard-bypass paths (empty-file rename target, delete-then-recreate, .feature exemption, different-payload retry, ticket lifetime, case/separator tricks, info disclosure, unbounded Set growth) all blocked at compile-time validation or explicitly declared in FR-3/FR-4/FR-5/NFR-SCOPE-1.

REVIEW_RESULT=PASS

Working...
