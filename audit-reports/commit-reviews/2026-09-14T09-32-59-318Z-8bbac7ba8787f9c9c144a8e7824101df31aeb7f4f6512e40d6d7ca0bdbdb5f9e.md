# OMP Review Kit commit review

- staged diff hash: 8bbac7ba8787f9c9c144a8e7824101df31aeb7f4f6512e40d6d7ca0bdbdb5f9e
- result: BLOCK
- reviewer models tried: @smol

## Normalized rejection envelope

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "review_failure",
  "diff_hash": "8bbac7ba8787f9c9c144a8e7824101df31aeb7f4f6512e40d6d7ca0bdbdb5f9e",
  "findings": [],
  "failure": {
    "code": "malformed_rejection_envelope",
    "message": "The rejection envelope was malformed or violated its schema."
  }
}
```

### Review coverage
- **Diff hash**: `8bbac7ba8787f9c9c144a8e7824101df31aeb7f4f6512e40d6d7ca0bdbdb5f9e`
- **Changed paths evaluated**:
  - `package.json`
  - `scripts/check-corpus-via-mcp.mjs`
  - `scripts/check-spec-corpus.mjs`
  - `scripts/compose-mri-lifecycle-receipts.mjs`
  - `scripts/corpus-root.mjs`
  - `scripts/create-release-evidence.mjs`
  - `scripts/dogfood-mcp.mjs`
  - `scripts/refresh-authoring-real-corpus-manifest.mjs`
  - `scripts/spec-graph-sync.mjs`
  - `scripts/verify-release.mjs`
- **Active review skills**: `skill://reality-first-review`, `skill://multi-stage-review`
- **Stages executed**:
  1. Stage 1 Context Scout (`review-context-scout`)
  2. Stage 2 Parallel Risk Hunters (`review-risk-hunter` in correctness and security lanes)
  3. Stage 3 Adversarial Verification (`review-finding-verifier`)
  4. Stage 4 Orchestrator Synthesis (`reviewer-kit`)

### Confirmed findings
- **Finding ID**: `correctness-1`
  - **Priority**: `P1`
  - **Defect class**: `correctness`
  - **File path**: `scripts/corpus-root.mjs`
  - **Line range**: `30-33`
  - **Observed behavior**: `corpusPath(relative, options)` returns `path.join(resolveCorpusRoot(options), relative)` without the `.specs` directory segment. When `resolveCorpusRoot(options)` returns either `$OMP_SPEC_KIT_ROOT` (which points to the repository root containing `.specs/`) or the default fixture path `tests/fixtures/kernel/authoring-real-corpus` (which contains `.specs/`), the returned path resolves directly under the repository root / fixture root instead of under `.specs/`.
  - **Expected behavior**: `corpusPath(relative, options)` should resolve paths inside the `.specs` directory (i.e. `path.join(resolveCorpusRoot(options), '.specs', relative)`), or ensure caller paths resolve to the actual specification files on disk.
  - **Trigger scenario**: Invoking `node scripts/create-release-evidence.mjs`, `node scripts/verify-release.mjs`, or `node scripts/compose-mri-lifecycle-receipts.mjs`.
  - **Impact**: All three release evidence and lifecycle scripts fail immediately with `ENOENT: no such file or directory` when attempting to read `plugin-distribution/plugin-distribution.feature`, blocking release packaging, evidence creation, and release verification against the frozen authoring fixture or external spec-kit root.
  - **Repository evidence**:
    - `scripts/corpus-root.mjs:31-33`: `export function corpusPath(relative, options = {}) { return path.join(resolveCorpusRoot(options), relative); }`
    - `scripts/compose-mri-lifecycle-receipts.mjs:67`: `corpusPath(path.join('plugin-distribution', 'plugin-distribution.feature'), { baseDir: repositoryRoot })`
    - `scripts/create-release-evidence.mjs:257`: `corpusPath(path.join('plugin-distribution', 'plugin-distribution.feature'), { baseDir: repositoryRoot })`
    - `scripts/verify-release.mjs:70`: `corpusPath(path.join('plugin-distribution', 'plugin-distribution.feature'), { baseDir: repositoryRoot })`
    - Snapshot filesystem: `tests/fixtures/kernel/authoring-real-corpus/.specs/plugin-distribution/plugin-distribution.feature` exists, while `tests/fixtures/kernel/authoring-real-corpus/plugin-distribution/plugin-distribution.feature` does not exist.

### Unproven/rejected summary
- **Security Lane**: Zero defect candidates. Subprocess arguments (`spawnSync`, `execFileSync`) use array arguments without shell interpolation; environment tokens are read cleanly and forwarded only via authorization headers; commit SHA validation in `refresh-authoring-real-corpus-manifest.mjs` enforces `/^[0-9a-f]{40}$/u`.

### Verified-OK
- `package.json`: Removal of `check:spec-corpus` from `verify` pipeline; addition of `check:corpus:remote`.
- `scripts/check-corpus-via-mcp.mjs`: MCP connection setup, token passing, tool invocation error handling and exit codes.
- `scripts/check-spec-corpus.mjs`: Strict check for corpus directory presence, failing closed when missing.
- `scripts/dogfood-mcp.mjs`: Stdio MCP child-process spawning and timeout handling against resolved corpus root.
- `scripts/refresh-authoring-real-corpus-manifest.mjs`: Strict hex commit validation and manifest metadata serialization.
- `scripts/spec-graph-sync.mjs`: Corpus root resolution via `resolveCorpusRoot` and graceful failure logging.

REVIEW_REJECTION_ENVELOPE_BEGIN
{"schema":"review-rejection-envelope@1","kind":"confirmed_findings","diff_hash":"8bbac7ba8787f9c9c144a8e7824101df31aeb7f4f6512e40d6d7ca0bdbdb5f9e","findings":[{"finding_id":"correctness-1","priority":"P1","defect_class":"correctness","file_path":"scripts/corpus-root.mjs","line_start":30,"line_end":33,"counterexample":"node scripts/create-release-evidence.mjs","verifier_argument":"In scripts/corpus-root.mjs:31-33, corpusPath(relative, options) computes path.join(resolveCorpusRoot(options), relative). resolveCorpusRoot() returns either raw (OMP_SPEC_KIT_ROOT, which is required to contain .specs) or tests/fixtures/kernel/authoring-real-corpus. In both cases, the actual .specs documents live inside .specs/, but corpusPath does not include the .specs segment. Callers in compose-mri-lifecycle-receipts.mjs, create-release-evidence.mjs, and verify-release.mjs call corpusPath(path.join('plugin-distribution', 'plugin-distribution.feature')), which resolves to tests/fixtures/kernel/authoring-real-corpus/plugin-distribution/plugin-distribution.feature rather than .../.specs/... triggering ENOENT."}]}
REVIEW_REJECTION_ENVELOPE_END

REVIEW_RESULT=BLOCK

Working...
