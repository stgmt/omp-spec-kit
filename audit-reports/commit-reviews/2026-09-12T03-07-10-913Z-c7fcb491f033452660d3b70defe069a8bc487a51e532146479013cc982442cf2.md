# OMP Review Kit commit review

- staged diff hash: c7fcb491f033452660d3b70defe069a8bc487a51e532146479013cc982442cf2
- result: BLOCK
- reviewer models tried: @slow, alibaba-token-plan/deepseek-v4-flash-0731, amd-radeon-cloud/DeepSeek-V4-Flash, byteplus-ark-coding/ark-code-latest

## Normalized rejection envelope

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "review_failure",
  "diff_hash": "c7fcb491f033452660d3b70defe069a8bc487a51e532146479013cc982442cf2",
  "findings": [],
  "failure": {
    "code": "missing_verdict_marker",
    "message": "No solitary review verdict marker was emitted."
  }
}
```

Reviewer-kit task **failed (exit 1)** — the agent's selected model is rate-limited.

## Failure

| Field | Value |
|---|---|
| Agent | `reviewer-kit` |
| Status | `failed (exit 1)` |
| Duration | 1.9s |
| Error | `429 Subscription quota exhausted` |
| Model | `muse-spark-1.3-contributor` (Meta) |
| Resets | 2026-09-14T00:00:00Z |

## Root cause

The reviewer-kit agent did **not** use the CLI-pinned `byteplus-ark-coding/ark-code-latest` model. Instead it selected `muse-spark-1.3-contributor`, which returned 429 (quota exhausted). The 1.9s duration confirms the call was rejected immediately — no review work started.

The staged diff `c7fcb49` was **not inspected**.

## To proceed, you have options

1. **Override the reviewer-kit model** — remove the restriction on `model`/`isolated` fields and pass `byteplus-ark-coding/ark-code-latest` explicitly.
2. **Wait** until the Meta quota resets at 2026-09-14T00:00:00Z and re-run.
3. **Use a different reviewer agent** (e.g. `reviewer` or a `task` agent) with an explicit model override.

What's your call?

Working...
