# OMP Review Kit commit review

- staged diff hash: f0a0110b0a3157b784d13470104a0e8c75c0e0f4283094c0977e67b345ae1902
- result: BLOCK
- reviewer models tried: devin/swe-2

## Normalized rejection envelope

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "confirmed_findings",
  "diff_hash": "f0a0110b0a3157b784d13470104a0e8c75c0e0f4283094c0977e67b345ae1902",
  "findings": [
    {
      "finding_id": "correctness-1",
      "priority": "P1",
      "defect_class": "correctness",
      "file_path": "tests/step-definitions/safe-authoring.steps.mjs",
      "line_start": 512,
      "line_end": 513,
      "verifier_argument": "Staged lines 512-513 sort res.data.error.documents with localeCompare and deepEqual against the server response, but src/authoring/elicitation-guard.js:43 emits code-point order and service.js:244-262 returns it verbatim. On the exact 14-document createSpec refusal list the orders diverge (new-feature-a_SCHEMA.md position; USE_CASES.md/USER_STORIES.md swap), so deepEqual throws deterministically on Node 22 — verified empirically; the sibling staged unit test pins code-point ordering.",
      "counterexample": "Run `npm run test:safe-authoring`: scenario 'elicitation-first-refusal' calls spec_patch createSpec for 'new-feature-a', receives the code-point-sorted refusal list, and assert.deepEqual fails at line 513."
    }
  ]
}
```

### Review coverage

- **Diff inspected**: staged diff `f0a0110b0a3157b784d13470104a0e8c75c0e0f4283094c0977e67b345ae1902` — 34 files, +1221/−27. Adds a first-write elicitation guard (`src/authoring/elicitation-guard.js`, wiring in `service.js`, `missing`/`beforeMissing` threading in `proposals.js`, hint re-export in `tool-contracts.js`), a new `.specs/agent-ux-elicitation-guard` corpus, a `spec-elicitation` skill, dist mirrors + manifest hashes, unit + BDD coverage, `test:unit` script, CI step, and corpus/package gate updates.
- **Stages executed**: context scout → parallel risk hunters (correctness, security) → adversarial verifier → local synthesis. All stages completed with valid output.
- **Skills used**: `reality-first-review`, `multi-stage-review` (mandatory); project knowledge applied: spec-elicitation contract (FR-1…FR-7, AC-1.1…AC-7.1 read via `spec_documents` MCP since `RAW_SPEC_WRITE` guards direct `.specs` reads), `omp-spec-kit` MCP tool surface.
- **Verified clean**: dist blobs byte-identical to staged src; all 34 manifest sha256 entries match staged blobs; guard sits inside `withWriteLock` after fingerprint + preimage checks; refusal writes zero bytes and is not cached in `this.applied` (retry re-executes and passes, per FR-4); `retryable:false` matches FR-4 "non-retryable by schema"; all other `createSpec`/first-creation callers use preview or were updated; output schema `error:{}` is free-form so `skill`/`documents` fields are schema-safe; no anti-parasitic form present (in-process domain guard, not a micro-CLI/inbox/receipt substitute).

### Confirmed findings

**correctness-1 — P1 — `tests/step-definitions/safe-authoring.steps.mjs:512-513`**

- **Observed**: the staged `elicitation-first-refusal` step computes `const sorted = [...res.data.error.documents].sort((a, b) => a.localeCompare(b))` and asserts `assert.deepEqual(res.data.error.documents, sorted)`. The SUT (`src/authoring/elicitation-guard.js:43`) emits the refusal document list in **code-point** order. For the `createSpec` "new-feature-a" refusal (14 `.md` docs), the orders diverge in two places: `new-feature-a_SCHEMA.md` is last under code-point but between `FR.md` and `NFR.md` under `localeCompare`, and `USE_CASES.md`/`USER_STORIES.md` swap. `deepEqual` throws deterministically (verified empirically on Node 22/ICU 78.3; holds under full-ICU and small-icu builds).
- **Expected**: the assertion must compare against the ordering the contract implements — code-point sort — which the sibling staged unit test (`tests/authoring-elicitation-edges.test.mjs`, "uses locale-independent code-point ordering") pins explicitly.
- **Trigger**: `npm run test:safe-authoring` (chained in `npm test`) → scenario `elicitation-first-refusal` → `assert.deepEqual` throws at line 513.
- **Impact**: the staged change's own verification scenario fails deterministically on every local `npm test` run; the commit cannot pass its declared verification gate (FR-7/AC-7.1) as staged. (CI `verify.yml` does not run this feature — the containerized bdd job executes only `release-evidence.feature` — so the failure is local-chain, not CI.)
- **Evidence**: `src/authoring/elicitation-guard.js:43`; `src/authoring/service.js:244-262` (documents returned verbatim, no re-sort); `src/authoring/proposals.js:479-487` + `src/kernel/types.js:9-23` (14-doc candidate set incl. `new-feature-a_SCHEMA.md`); staged `tests/step-definitions/safe-authoring.steps.mjs:512-513`; `tests/features/safe-authoring.feature:95-98`; `package.json:18-19`; empirical comparator run. Note: the working tree already contains an unstaged fix for this exact line — the staged blob remains defective.

### Unproven/rejected summary

- **Security lane**: zero candidates. Guard only refuses (never permits); runs after slug validation, canonical allowlist, secret detection, and lock-time preimage checks; `error.documents` discloses nothing beyond what `spec_documents`/preview already expose; message interpolation uses canonical-allowlisted names only; ticket `Set` growth requires a full compile + write-lock refusal per entry; SKILL.md is a benign procedural memo; no CI/script injection surface.
- **Correctness lane (evaluated, not raised)**: ticket-on-refusal-before-commit ordering (spec-sanctioned), stale `beforeMissing` closed by preimage check, rename-into-existing-empty-file exemption (FR-6), refusal not replay-cached (required for retry), in-memory ticket scope across restarts (declared intentional in DESIGN.md), mutation-gate receipt timestamp (regeneration artifact, not a coverage claim).

```text
REVIEW_REJECTION_ENVELOPE_BEGIN
{"schema":"review-rejection-envelope@1","kind":"confirmed_findings","diff_hash":"f0a0110b0a3157b784d13470104a0e8c75c0e0f4283094c0977e67b345ae1902","findings":[{"finding_id":"correctness-1","priority":"P1","defect_class":"correctness","file_path":"tests/step-definitions/safe-authoring.steps.mjs","line_start":512,"line_end":513,"verifier_argument":"Staged lines 512-513 sort res.data.error.documents with localeCompare and deepEqual against the server response, but src/authoring/elicitation-guard.js:43 emits code-point order and service.js:244-262 returns it verbatim. On the exact 14-document createSpec refusal list the orders diverge (new-feature-a_SCHEMA.md position; USE_CASES.md/USER_STORIES.md swap), so deepEqual throws deterministically on Node 22 — verified empirically; the sibling staged unit test pins code-point ordering.","counterexample":"Run `npm run test:safe-authoring`: scenario 'elicitation-first-refusal' calls spec_patch createSpec for 'new-feature-a', receives the code-point-sorted refusal list, and assert.deepEqual fails at line 513."}]}
REVIEW_REJECTION_ENVELOPE_END
REVIEW_RESULT=BLOCK
```

Working...
