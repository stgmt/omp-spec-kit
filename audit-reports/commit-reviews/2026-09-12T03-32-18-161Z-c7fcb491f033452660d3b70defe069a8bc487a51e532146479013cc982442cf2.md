# OMP Review Kit commit review

- staged diff hash: c7fcb491f033452660d3b70defe069a8bc487a51e532146479013cc982442cf2
- result: BLOCK
- reviewer models tried: devin/swe-2

## Normalized rejection envelope

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "confirmed_findings",
  "diff_hash": "c7fcb491f033452660d3b70defe069a8bc487a51e532146479013cc982442cf2",
  "findings": [
    {
      "finding_id": "correctness-1",
      "priority": "P1",
      "defect_class": "correctness",
      "file_path": "plugins/omp-spec-kit/dist/manifest.json",
      "line_start": 101,
      "line_end": 103,
      "verifier_argument": "Recomputed sha256 of staged dist/kernel/types.js is 26f1248a… while manifest.json:102 attests 815baeeb…, the hash of stray staged types.js.full (16-descriptor ROADMAP variant). verify-package.mjs:306 fails 'dist hash mismatch for kernel/types.js' on any clean checkout; CI masks it because verify.yml runs npm run build (rm dist + regenerate manifest) first; release dirty-check (create-release-candidate.mjs:46-50) blocks the tag.",
      "counterexample": "Run npm run verify on a clean checkout of this commit without rebuilding: deterministic 'dist hash mismatch for kernel/types.js' failure."
    },
    {
      "finding_id": "security-1",
      "priority": "P1",
      "defect_class": "security",
      "file_path": "scripts/spec-graph-sync.mjs",
      "line_start": 490,
      "line_end": 524,
      "verifier_argument": "startServe binds HTTP on 127.0.0.1:8787 with zero authentication, no Origin/Host check, no shared secret; shape-only validateWritebackEvent (specId contains ':', issueId required but never correlated) forwards to spec_patch setEntityStatus dryRun:false rewriting TASKS.md Status lines; unknown toState still mutates via 'todo' default; each request spawns a fresh MCP child. spec-writeback.js:14,36-41 authenticates with nothing — no secret exists to forge. --serve is a parsed flag requiring no token (parseArgs:480).",
      "counterexample": "While --serve runs, any webpage in the operator's browser executes fetch('http://127.0.0.1:8787/writeback',{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain'},body:'{\"specId\":\"omp-spec-kit:TASK-1\",\"toState\":\"Fixed\",\"issueId\":\"x\"}'}) — a CORS-simple request with no preflight — flipping TASK-1 to done; any local process or Docker container via host.docker.internal can replay forged events."
    },
    {
      "finding_id": "correctness-2",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "plugins/omp-spec-kit/dist/kernel/types.js.full",
      "line_start": 55,
      "line_end": 58,
      "verifier_argument": "git ls-files --stage confirms types.js.c1 (same blob as types.js) and types.js.full (divergent 16-descriptor variant, ROADMAP at line 55 absent from staged src) are staged inside the plugin payload; neither is in manifest.json, so committed dist is not the closed manifest-listed set the build contract requires. verify-package.mjs:224 assertExactList and the release dirty-check both fail on this commit.",
      "counterexample": "Any consumer reading the committed dist directory directly encounters a divergent 16-descriptor copy of the kernel type universe beside the real one; verify-package's exact-file-list check fails on this commit until dist is rebuilt."
    },
    {
      "finding_id": "correctness-3",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "docs/validation/mutation-gate-receipt.json",
      "line_start": 739,
      "line_end": 745,
      "verifier_argument": "Receipt records totalMutants=165 incl. delete-entity-descriptor-ROADMAP killed by 'must not drop / 15 !== 16', but staged check-tool-surface-mutations.mjs:331 asserts 'must equal 15' and staged types.js has 15 descriptors — the staged gate generates 164 mutants and can never emit that assertion or mutant. Receipt is write-only (:569-571 overwrites; nothing verifies the committed copy).",
      "counterexample": "Re-run npm run check:mutations on the staged tree: produces 164 mutants, different killerAssertion strings, and no ROADMAP mutant — the committed receipt documents a different code state than the commit contains."
    },
    {
      "finding_id": "correctness-4",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "tests/spec-graph-sync/sync.test.js",
      "line_start": 1,
      "line_end": 10,
      "verifier_argument": "tests/spec-graph-sync/{sync,writeback}.test.js use node:test but no package.json script references them (test = build+verify+dogfood+safe-authoring+staged+bdd, all cucumber runners) and no .github/workflows job runs node --test; verify.yml runs build/verify/docker-bdd/portability only.",
      "counterexample": "Any regression in buildProjectionPlan, compareProjection, sync ordering, or validateWritebackEvent passes npm test and CI green because the only tests covering them are never invoked."
    },
    {
      "finding_id": "correctness-7",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "scripts/spec-graph-sync.mjs",
      "line_start": 497,
      "line_end": 513,
      "verifier_argument": "The async createServer listener awaits for-await over req (497) and mkdir/appendFile (512-513) outside any try/catch; only the spec_patch call is wrapped (514-522). No process.on('unhandledRejection') exists in the file; Node 22 defaults unhandledRejection to throw. spec-writeback.js:41-43 only console.warns on unreachable listener.",
      "counterexample": "POST /writeback then disconnect before the body finishes (or make tmpdir unwritable): the rejection kills the --serve daemon and every subsequent tracker writeback event is silently lost."
    },
    {
      "finding_id": "correctness-8",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "src/adapters/youtrack-projection.js",
      "line_start": 122,
      "line_end": 123,
      "verifier_argument": "specStatusForTrackerState returns REVERSE_STATUS_MAP[state] ?? 'todo'; the map covers 7 states so 'Won't fix', 'Duplicate', 'Can't reproduce' and custom states all become 'todo'. spec-writeback.js:27-30 fires on ANY State change with no allowlist; spec-graph-sync.mjs:515-516 applies the mapped status unconditionally; :317 then commands State Open (STATUS_MAP[todo]) on the next sync.",
      "counterexample": "Move a SPEC task card to 'Won't fix' → onChange POSTs toState='Won't fix' → spec_patch writes status=todo into TASKS.md → next sync commands State Open, resurrecting the deliberately closed card on every change+sync cycle."
    },
    {
      "finding_id": "correctness-9",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "scripts/spec-graph-sync.mjs",
      "line_start": 316,
      "line_end": 316,
      "verifier_argument": "upsertCards patches Evidence only when wanted.evidence is truthy, so removing the attribute leaves the stale tracker value forever; compareProjection includes evidence in the stableJson card comparison (youtrack-projection.js:427,444) so parity reports CARD_DRIFT on every run and the canSkip zero-write path (:482-484 requires parity.equal) is permanently unreachable for that card.",
      "counterexample": "Remove the Evidence attribute from a spec node → sync reports CARD_DRIFT, rewrites summary/description, never clears Evidence → every later sync repeats the full upsert/reconcile/stale/publish cycle without converging."
    },
    {
      "finding_id": "correctness-10",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "scripts/spec-graph-sync.mjs",
      "line_start": 63,
      "line_end": 98,
      "verifier_argument": "callTool spawns the MCP server (63) with no child.on('error') and writes to child.stdin (98) with no stdin error handler; the 'close' handler (85-89) rejects pending waiters but does not consume the stream 'error' event; no timeout bounds any request (62-117). resolveMcpServer (36-46) prefers the installed plugin dist, a plausible crash-on-startup source.",
      "counterexample": "The resolved server crashes on startup: the write to a closing stdin raises unhandled EPIPE → process crash; a wedged server leaves the promise unsettled forever, and inside /writeback also hangs the HTTP request."
    },
    {
      "finding_id": "correctness-11",
      "priority": "P2",
      "defect_class": "correctness",
      "file_path": "tools/spec-graph-app/widgets/spec-panel/index.html",
      "line_start": 197,
      "line_end": 198,
      "verifier_argument": "The panel renders Approve for every TASK with issue.resolved false regardless of current State, while BUTTON_TRANSITIONS (youtrack-projection.js:35) declares Approve as Open→Fixed and FR-7 requires the workflow to validate identity and transition; click sends 'State Fixed' via commands (206-209) and writeback then sets spec status done.",
      "counterexample": "Open a SPEC task card in state Submitted (spec status planned) → panel shows Approve → click sends 'State Fixed' → writeback sets spec status done, skipping the tracked Open stage entirely."
    },
    {
      "finding_id": "security-2",
      "priority": "P2",
      "defect_class": "security",
      "file_path": "scripts/spec-graph-sync.mjs",
      "line_start": 297,
      "line_end": 300,
      "verifier_argument": "upsertCards builds YouTrack command queries by raw concatenation command(query+' '+value) on the create path (300, includes ['Evidence', wanted.evidence]) and command(field+' '+value) on the patch path (319); wanted.evidence is free text lifted verbatim from spec markdown attributes (service.js:739; buildProjectionPlan:194 unfiltered) and never quoted; YouTrack's command parser treats space-separated tokens as additional commands. Evidence is the only free-text field on this path.",
      "counterexample": "A spec author writes 'Evidence: x State Verified' in a spec file → next sync issues 'Evidence x State Verified' to /api/commands under the sync token's privileges, executing the smuggled State command on the freshly created/patched issue."
    }
  ]
}
```

The task result was truncated; the full report was recovered from the agent transcript (`history://AcuteAardvark`). Reproducing verbatim:

---

All stages complete. The verifier confirmed 11 findings (2×P1, 9×P2), rejected 1 (correctness-12, subsumed by security-1), and marked 2 not_proven (correctness-5, correctness-6 — no reachable scoped-sync caller). Note: the verifier's coverage_summary says "9 findings" but its decisions/confirmed_findings arrays authoritatively contain 11 confirmed — I use the arrays.

**Stage 4: Orchestrator Synthesis**

### Review coverage

- **Diff**: staged change in `E:/repos/omp-spec-kit`, hash `c7fcb491f033452660d3b70defe069a8bc487a51e532146479013cc982442cf2` (verified). 46 staged paths, +25,624/−242. `.specs/spec-mcp-operations/*` modifications are **unstaged** and were excluded; untracked files excluded.
- **Change**: adds `spec_graph` `view:"board"` (BoardProjectionV1) in kernel + adapters; new `src/adapters/youtrack-projection.js`; new `scripts/spec-graph-sync.mjs` (MCP→YouTrack sync + `--serve` writeback listener on 127.0.0.1:8787); new `tools/spec-graph-app/` YouTrack app (manifest v1.0.18, spec-board/spec-panel widgets, spec-writeback.js rule, prototypes); new `.specs/youtrack-visualization/` corpus; `tests/spec-graph-sync/` node:test files; `@jetbrains/youtrack-apps-tools` dep; dist mirror + manifest; README/ROADMAP/validation-receipt updates.
- **Skills used**: `reality-first-review`, `multi-stage-review`, `youtrack-visualization-spec-review-and-patch`, `youtrack-board-kernel-source`, `youtrack-board-widget-debugging`, `youtrack-spec-board-publish-verify`, `tool-e2e`, `omp-spec-kit-spec-authoring`.
- **Stages**: Scout (1 blocking task) → parallel risk hunters (correctness + security, 14 candidates) → adversarial verifier (14 decisions) → local synthesis. All stages produced valid output.

### Confirmed findings

**P1 — `correctness-1` — dist manifest attests the wrong types.js hash** — `plugins/omp-spec-kit/dist/manifest.json:101-103`
- Observed: manifest records `kernel/types.js` sha256 `815baeeb…`, which is the hash of the stray staged `types.js.full` (16-descriptor variant with ROADMAP). The actual staged payload `types.js` hashes `26f1248a…` (15 descriptors, byte-identical to staged `src/kernel/types.js`).
- Expected: committed manifest must hash committed payload bytes; `verify-package.mjs:306` enforces `distManifest.files[name].sha256 === sha256(distBytes)`.
- Trigger: `npm run verify` on a clean checkout without rebuilding → deterministic `dist hash mismatch for kernel/types.js`. CI masks it because `verify.yml` runs `npm run build` (rm dist + regenerate manifest) first.
- Impact: the shipped artifact's integrity manifest is false for the file defining the entity-type universe; the release dirty-check (`create-release-candidate.mjs:46-50`) blocks the tag until dist is rebuilt and recommitted.
- Evidence: recomputed sha256 of staged blobs; `verify-package.mjs:306`; `build-plugin.mjs:103,152`.

**P1 — `security-1` — unauthenticated loopback `/writeback` mutates any spec's TASK status via spec_patch** — `scripts/spec-graph-sync.mjs:490-524`
- Observed: `startServe()` binds HTTP on 127.0.0.1:8787 with zero authentication, no Origin/Host check, no shared secret. Any POST passing shape-only `validateWritebackEvent` (specId merely contains `:`; issueId required but never correlated) reaches `spec_patch setEntityStatus dryRun:false`, rewriting the Status line in that spec's TASKS.md. Unknown `toState` still mutates via the `'todo'` default; each request spawns a fresh MCP server child.
- Expected: an endpoint writing into the spec corpus (declared sole source of truth) must authenticate the caller and reject unknown states.
- Trigger: while `--serve` runs (parsed flag needing no token), any webpage in the operator's browser fires a CORS-simple `no-cors` POST with a `text/plain` JSON body — no preflight, body parsed regardless of Content-Type — flipping arbitrary `slug:TASK-n` statuses; any local process or Docker container via `host.docker.internal` can do the same.
- Impact: silent unauthenticated task-status mutation across every spec (done↔todo↔planned), corrupting spec↔tracker parity; per-request MCP spawn amplifies resource exhaustion. Blast radius bounded to TASK status by `setEntityStatus→replace_task_status`.
- Evidence: `spec-graph-sync.mjs:490-524,139-153,480`; `spec-writeback.js:14,36-41` (no credential exists to forge); `youtrack-projection.js:122-132`; `src/authoring/proposals.js:367-378`.

**P2 — `correctness-2` — stray payload files `types.js.c1`/`types.js.full` staged inside dist** — `plugins/omp-spec-kit/dist/kernel/types.js.full:55-58` (and `types.js.c1`)
- Observed: two files staged in the plugin payload — `.c1` (same blob as types.js) and `.full` (types.js + extra ROADMAP descriptor at line 55). Neither is in `manifest.json`; committed dist is not the closed manifest-listed set the build produces.
- Expected: dist payload = closed `SOURCE_FILES`+manifest set (`build-plugin.mjs` rm+rebuild).
- Trigger: any consumer reading committed dist directly encounters a divergent 16-descriptor copy of the kernel type universe beside the real one; `verify-package.mjs:224` `assertExactList` and the release dirty-check both fail on this commit.
- Impact: dead payload + divergent-truth copy of kernel types inside the committed artifact.
- Evidence: `git ls-files --stage` blobs `86fd1eab`/`ac2c1cf5`; `types.js.full:55` `kind:"ROADMAP"` absent from staged src; manifest has no `*.c1/*.full` keys.

**P2 — `correctness-3` — staged mutation-gate receipt is unreproducible from the staged tree** — `docs/validation/mutation-gate-receipt.json:739-745`
- Observed: receipt claims `totalMutants=165` incl. `delete-entity-descriptor-ROADMAP` killed by `Entity descriptor count must not drop\n\n15 !== 16`. Staged `check-tool-surface-mutations.mjs:331` asserts `must equal 15` and staged types.js has 15 descriptors — the staged gate generates 164 mutants and can never emit that assertion or mutant.
- Expected: a committed gate receipt must be reproducible by the staged gate script against staged sources.
- Trigger: re-run `npm run check:mutations` on the staged tree → 164 mutants, different killer assertions, no ROADMAP mutant.
- Impact: stale/false gate evidence — the receipt attests coverage (incl. `delete-branch-spec_graph-board` KILLED) produced by a script+types combination this commit does not ship.
- Evidence: receipt `:4,:739-745`; staged script `:324-331`; receipt is write-only (`:569-571` overwrites; nothing verifies the committed copy).

**P2 — `correctness-4` — new spec-graph-sync tests wired into no npm script and no CI job** — `tests/spec-graph-sync/sync.test.js:1-10` (and `writeback.test.js`)
- Observed: both files use `node:test`, but no package.json script references them (`test` = build+verify+dogfood+safe-authoring+staged+bdd, all cucumber/feature runners) and no workflow runs `node --test`.
- Expected: tests covering the new board/projection/writeback surface must execute in the pipeline.
- Trigger: any regression in `buildProjectionPlan`/`compareProjection`/sync ordering/`validateWritebackEvent` passes `npm test` and CI green.
- Impact: the entire new behavior surface ships with zero executable test proof in the pipeline.
- Evidence: staged `package.json` scripts; grep over `package.json`/`.github/workflows`/`scripts` finds no runner; `verify.yml` runs build/verify/docker-bdd/portability only.

**P2 — `correctness-7` — writeback HTTP handler crashes the daemon on unhandled async errors** — `scripts/spec-graph-sync.mjs:497-513`
- Observed: the async `createServer` listener awaits `for await (const chunk of req)` (497) and `mkdir`/`appendFile` (512-513) outside any try/catch; only the spec_patch call is wrapped (514-522). No `process.on('unhandledRejection')` exists in the file.
- Expected: a long-lived listener must contain per-request failures.
- Trigger: POST `/writeback` then disconnect mid-body (or unwritable tmpdir) → iterator/appendFile rejection → Node 22 default `unhandledRejection=throw` kills `--serve`; `spec-writeback.js:41-43` only `console.warn`s, so all later writeback events are silently lost.
- Impact: writeback daemon dies on a routine network/fs fault; tracker↔spec sync stops with no recovery and no loss marker.
- Evidence: `spec-graph-sync.mjs:497,512-514`; no rejection handlers in file; `spec-writeback.js:41-43`; `package.json` engines node 22.

**P2 — `correctness-8` — unmapped tracker states write `todo` into the spec and reopen the card** — `src/adapters/youtrack-projection.js:122-123`
- Observed: `specStatusForTrackerState` returns `REVERSE_STATUS_MAP[state] ?? 'todo'`; the map covers 7 states, so `Won't fix`, `Duplicate`, `Can't reproduce`, and any custom state all become `todo`. `spec-writeback.js` fires on ANY State change; `validateWritebackEvent` does not restrict `toState`.
- Expected: an unrecognized terminal state must not be rewritten as an open task.
- Trigger: move a SPEC card to `Won't fix` → onChange POSTs `toState='Won't fix'` → listener calls `spec_patch setEntityStatus status=todo` → spec corrupted → next sync sees `STATUS_MAP[todo]='Open'` ≠ `Won't fix` and commands `State Open`, resurrecting the card every cycle.
- Impact: spec data corruption plus a tracker fight loop on deliberately closed tasks.
- Evidence: `youtrack-projection.js:122-123,25-33`; `spec-writeback.js:27-30`; `spec-graph-sync.mjs:515-516,317`.

**P2 — `correctness-9` — Evidence field can never be cleared: perpetual CARD_DRIFT** — `scripts/spec-graph-sync.mjs:316`
- Observed: `upsertCards` patches Evidence only when `wanted.evidence` is truthy; removing the attribute leaves the stale tracker value forever, while `compareProjection` counts evidence in the card comparison → CARD_DRIFT on every subsequent run.
- Expected: a cleared field must converge to empty; same-fingerprint drift repaired deterministically (NFR-IDEMPOTENCE-1).
- Trigger: remove `Evidence` from a spec node → every later sync repeats the full upsert; the zero-write skip path (`canSkip` requires `parity.equal`) is unreachable for that card.
- Impact: permanent drift; stale evidence stays forever; every sync performs the full write cycle without converging.
- Evidence: `spec-graph-sync.mjs:316`; `youtrack-projection.js:427,444,451,482-484`.

**P2 — `correctness-10` — McpStdioClient crashes on child stdin EPIPE and hangs forever on a stuck server** — `scripts/spec-graph-sync.mjs:63-98`
- Observed: `callTool` spawns the MCP server with no `child.on('error')` and writes to `child.stdin` with no error handler; the `close` handler rejects pending waiters but does not consume the stream `error` event. No timeout bounds any request.
- Expected: a transport client must tolerate early child exit and bound wait time.
- Trigger: resolved server (preferring the installed plugin dist, which can be stale/broken) crashes on startup → write to closing stdin raises unhandled EPIPE → process crash; a wedged server leaves the promise unsettled forever — inside `/writeback` this also hangs the HTTP request.
- Impact: sync/writeback crash or unbounded hang on a routine child-process failure mode.
- Evidence: `spec-graph-sync.mjs:63-67,85-89,98,62-117,36-46`.

**P2 — `correctness-11` — panel offers Approve for any unresolved TASK, bypassing the declared Open→Fixed transition** — `tools/spec-graph-app/widgets/spec-panel/index.html:197-198`
- Observed: Approve renders for every TASK with `issue.resolved === false`, regardless of current State; `BUTTON_TRANSITIONS` declares Approve as Open→Fixed.
- Expected: button availability follows the declared transition table (Approve only from Open), per FR-7 "the workflow SHALL validate identity and transition".
- Trigger: open a SPEC card in `Submitted` (spec status planned) → panel shows Approve → click sends `State Fixed` → writeback sets spec status `done`, skipping the tracked Open stage.
- Impact: ungoverned status jumps; planned tasks become done without entering Open, desyncing the intended workflow.
- Evidence: `spec-panel/index.html:197,206-209`; `youtrack-projection.js:35`; youtrack-visualization FR-7.

**P2 — `security-2` — YouTrack command injection via unquoted spec Evidence attribute** — `scripts/spec-graph-sync.mjs:297-300` (also `:316-319`)
- Observed: `upsertCards` builds command queries by raw concatenation `command(query + ' ' + value)` including `['Evidence', wanted.evidence]`; `wanted.evidence` is free text lifted verbatim from spec markdown attributes, never quoted/escaped. YouTrack's command parser treats space-separated tokens as additional commands.
- Expected: free-text spec content in a tracker command query must be quoted/escaped, or set via a dedicated custom-field endpoint.
- Trigger: a spec author writes `Evidence: x State Verified` in a spec file → next sync issues `Evidence x State Verified` to `/api/commands` under the sync token's privileges, executing the smuggled State command; benign multi-word evidence also silently breaks the field-set.
- Impact: anyone with commit access to the spec corpus executes arbitrary YouTrack commands with the sync operator's token privileges. Evidence is the only free-text field on this path; other fields are enum/map-constrained.
- Evidence: `spec-graph-sync.mjs:293-300,316-319,201-206`; `src/kernel/query/service.js:739`; `youtrack-projection.js:194`.

### Unproven/rejected summary

- `correctness-5` (runBoard drops unknown specSlugs, `complete:true`) — **not_proven**: lenient `selectedSpecs` is the pre-existing convention shared by runInventory/overview; the response honestly reports the matched `scope.specSlugs`; no staged contract requires NOT_FOUND; the data-loss impact needs the unreachable scoped-sync path.
- `correctness-6` (scoped sync deletes out-of-scope cards) — **not_proven**: code path exists but no reachable caller passes `specSlugs` — `main()` calls `sync({dryRun, log})` and `parseArgs` rejects unknown arguments. Latent dead-parameter behavior.
- `correctness-12` (issueId required but unbound) — **rejected**: no independent incorrect behavior; legitimate rule sends consistent issueId/SpecId; misattribution requires forged events, fully subsumed by `security-1`.

REVIEW_REJECTION_ENVELOPE_BEGIN
{"schema":"review-rejection-envelope@1","kind":"confirmed_findings","diff_hash":"c7fcb491f033452660d3b70defe069a8bc487a51e532146479013cc982442cf2","findings":[{"finding_id":"correctness-1","priority":"P1","defect_class":"correctness","file_path":"plugins/omp-spec-kit/dist/manifest.json","line_start":101,"line_end":103,"verifier_argument":"Recomputed sha256 of staged dist/kernel/types.js is 26f1248a… while manifest.json:102 attests 815baeeb…, the hash of stray staged types.js.full (16-descriptor ROADMAP variant). verify-package.mjs:306 fails 'dist hash mismatch for kernel/types.js' on any clean checkout; CI masks it because verify.yml runs npm run build (rm dist + regenerate manifest) first; release dirty-check (create-release-candidate.mjs:46-50) blocks the tag.","counterexample":"Run npm run verify on a clean checkout of this commit without rebuilding: deterministic 'dist hash mismatch for kernel/types.js' failure."},{"finding_id":"security-1","priority":"P1","defect_class":"security","file_path":"scripts/spec-graph-sync.mjs","line_start":490,"line_end":524,"verifier_argument":"startServe binds HTTP on 127.0.0.1:8787 with zero authentication, no Origin/Host check, no shared secret; shape-only validateWritebackEvent (specId contains ':', issueId required but never correlated) forwards to spec_patch setEntityStatus dryRun:false rewriting TASKS.md Status lines; unknown toState still mutates via 'todo' default; each request spawns a fresh MCP child. spec-writeback.js:14,36-41 authenticates with nothing — no secret exists to forge. --serve is a parsed flag requiring no token (parseArgs:480).","counterexample":"While --serve runs, any webpage in the operator's browser executes fetch('http://127.0.0.1:8787/writeback',{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain'},body:'{\"specId\":\"omp-spec-kit:TASK-1\",\"toState\":\"Fixed\",\"issueId\":\"x\"}'}) — a CORS-simple request with no preflight — flipping TASK-1 to done; any local process or Docker container via host.docker.internal can replay forged events."},{"finding_id":"correctness-2","priority":"P2","defect_class":"correctness","file_path":"plugins/omp-spec-kit/dist/kernel/types.js.full","line_start":55,"line_end":58,"verifier_argument":"git ls-files --stage confirms types.js.c1 (same blob as types.js) and types.js.full (divergent 16-descriptor variant, ROADMAP at line 55 absent from staged src) are staged inside the plugin payload; neither is in manifest.json, so committed dist is not the closed manifest-listed set the build contract requires. verify-package.mjs:224 assertExactList and the release dirty-check both fail on this commit.","counterexample":"Any consumer reading the committed dist directory directly encounters a divergent 16-descriptor copy of the kernel type universe beside the real one; verify-package's exact-file-list check fails on this commit until dist is rebuilt."},{"finding_id":"correctness-3","priority":"P2","defect_class":"correctness","file_path":"docs/validation/mutation-gate-receipt.json","line_start":739,"line_end":745,"verifier_argument":"Receipt records totalMutants=165 incl. delete-entity-descriptor-ROADMAP killed by 'must not drop / 15 !== 16', but staged check-tool-surface-mutations.mjs:331 asserts 'must equal 15' and staged types.js has 15 descriptors — the staged gate generates 164 mutants and can never emit that assertion or mutant. Receipt is write-only (:569-571 overwrites; nothing verifies the committed copy).","counterexample":"Re-run npm run check:mutations on the staged tree: produces 164 mutants, different killerAssertion strings, and no ROADMAP mutant — the committed receipt documents a different code state than the commit contains."},{"finding_id":"correctness-4","priority":"P2","defect_class":"correctness","file_path":"tests/spec-graph-sync/sync.test.js","line_start":1,"line_end":10,"verifier_argument":"tests/spec-graph-sync/{sync,writeback}.test.js use node:test but no package.json script references them (test = build+verify+dogfood+safe-authoring+staged+bdd, all cucumber runners) and no .github/workflows job runs node --test; verify.yml runs build/verify/docker-bdd/portability only.","counterexample":"Any regression in buildProjectionPlan, compareProjection, sync ordering, or validateWritebackEvent passes npm test and CI green because the only tests covering them are never invoked."},{"finding_id":"correctness-7","priority":"P2","defect_class":"correctness","file_path":"scripts/spec-graph-sync.mjs","line_start":497,"line_end":513,"verifier_argument":"The async createServer listener awaits for-await over req (497) and mkdir/appendFile (512-513) outside any try/catch; only the spec_patch call is wrapped (514-522). No process.on('unhandledRejection') exists in the file; Node 22 defaults unhandledRejection to throw. spec-writeback.js:41-43 only console.warns on unreachable listener.","counterexample":"POST /writeback then disconnect before the body finishes (or make tmpdir unwritable): the rejection kills the --serve daemon and every subsequent tracker writeback event is silently lost."},{"finding_id":"correctness-8","priority":"P2","defect_class":"correctness","file_path":"src/adapters/youtrack-projection.js","line_start":122,"line_end":123,"verifier_argument":"specStatusForTrackerState returns REVERSE_STATUS_MAP[state] ?? 'todo'; the map covers 7 states so 'Won't fix', 'Duplicate', 'Can't reproduce' and custom states all become 'todo'. spec-writeback.js:27-30 fires on ANY State change with no allowlist; spec-graph-sync.mjs:515-516 applies the mapped status unconditionally; :317 then commands State Open (STATUS_MAP[todo]) on the next sync.","counterexample":"Move a SPEC task card to 'Won't fix' → onChange POSTs toState='Won't fix' → spec_patch writes status=todo into TASKS.md → next sync commands State Open, resurrecting the deliberately closed card on every change+sync cycle."},{"finding_id":"correctness-9","priority":"P2","defect_class":"correctness","file_path":"scripts/spec-graph-sync.mjs","line_start":316,"line_end":316,"verifier_argument":"upsertCards patches Evidence only when wanted.evidence is truthy, so removing the attribute leaves the stale tracker value forever; compareProjection includes evidence in the stableJson card comparison (youtrack-projection.js:427,444) so parity reports CARD_DRIFT on every run and the canSkip zero-write path (:482-484 requires parity.equal) is permanently unreachable for that card.","counterexample":"Remove the Evidence attribute from a spec node → sync reports CARD_DRIFT, rewrites summary/description, never clears Evidence → every later sync repeats the full upsert/reconcile/stale/publish cycle without converging."},{"finding_id":"correctness-10","priority":"P2","defect_class":"correctness","file_path":"scripts/spec-graph-sync.mjs","line_start":63,"line_end":98,"verifier_argument":"callTool spawns the MCP server (63) with no child.on('error') and writes to child.stdin (98) with no stdin error handler; the 'close' handler (85-89) rejects pending waiters but does not consume the stream 'error' event; no timeout bounds any request (62-117). resolveMcpServer (36-46) prefers the installed plugin dist, a plausible crash-on-startup source.","counterexample":"The resolved server crashes on startup: the write to a closing stdin raises unhandled EPIPE → process crash; a wedged server leaves the promise unsettled forever, and inside /writeback also hangs the HTTP request."},{"finding_id":"correctness-11","priority":"P2","defect_class":"correctness","file_path":"tools/spec-graph-app/widgets/spec-panel/index.html","line_start":197,"line_end":198,"verifier_argument":"The panel renders Approve for every TASK with issue.resolved false regardless of current State, while BUTTON_TRANSITIONS (youtrack-projection.js:35) declares Approve as Open→Fixed and FR-7 requires the workflow to validate identity and transition; click sends 'State Fixed' via commands (206-209) and writeback then sets spec status done.","counterexample":"Open a SPEC task card in state Submitted (spec status planned) → panel shows Approve → click sends 'State Fixed' → writeback sets spec status done, skipping the tracked Open stage entirely."},{"finding_id":"security-2","priority":"P2","defect_class":"security","file_path":"scripts/spec-graph-sync.mjs","line_start":297,"line_end":300,"verifier_argument":"upsertCards builds YouTrack command queries by raw concatenation command(query+' '+value) on the create path (300, includes ['Evidence', wanted.evidence]) and command(field+' '+value) on the patch path (319); wanted.evidence is free text lifted verbatim from spec markdown attributes (service.js:739; buildProjectionPlan:194 unfiltered) and never quoted; YouTrack's command parser treats space-separated tokens as additional commands. Evidence is the only free-text field on this path.","counterexample":"A spec author writes 'Evidence: x State Verified' in a spec file → next sync issues 'Evidence x State Verified' to /api/commands under the sync token's privileges, executing the smuggled State command on the freshly created/patched issue."}]}
REVIEW_REJECTION_ENVELOPE_END
REVIEW_RESULT=BLOCK

Working...
