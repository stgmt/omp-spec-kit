# OMP Review Kit commit review

- staged diff hash: d6ebef47ab826b6684b6df7ce0487986062eeacb099645c1721d7054cdac07a9
- result: PASS
- reviewer models tried: @smol

### Review coverage
- **Staged Snapshot**: `C:\Users\stigm\AppData\Local\Temp\reviewer-kit-snapshot-FLuj9W`
- **Staged Diff Hash**: `d6ebef47ab826b6684b6df7ce0487986062eeacb099645c1721d7054cdac07a9`
- **Changed Paths Inspected (11)**:
  - `package.json`
  - `src/adapters/tool-contracts.js`
  - `src/service/git.js`
  - `src/service/index.js`
  - `src/service/ledger.js`
  - `src/service/publish.js`
  - `src/service/registry.js`
  - `src/service/sync.js`
  - `src/service/versioned.js`
  - `src/service/writepath.js`
  - `tests/service/publish.test.mjs`
- **Callers and Downstream Consumers Inspected (8)**:
  - `src/service/index.js` (wires `createPublisher` and `createVersionedReads` into `startService` and `buildServiceStack`, invokes boot-time `publisher.publishAll()`, registers `publishAll` in sync `afterReconcile`, hooks drift report to inspect `access_log` for `publish-rejected:` events)
  - `src/service/writepath.js` (invokes `publisher.publishSpec` asynchronously post-push and post-reconcile in `createWritePipeline.specPatch`)
  - `src/service/sync.js` (invokes `afterReconcile` callback in `startSync` on fast-forward remote reconciliations)
  - `src/service/dispatch.js` (dispatches `spec_documents` tool calls through `wrappers.documents` when `version` argument is supplied)
  - `src/service/http.js` (forwards wrappers to `createDispatcher`)
  - `src/service/ledger.js` (`SqliteStore` and `JsonlStore` backing `insertLedgerIfAbsent`, `getLedger`, and `access_log` persistence)
  - `src/service/registry.js` (exports `authoredField` helper used by `publish.js`, incorporates ledger entries into `registryIndex`)
  - `src/service/ops/registry.js` (`spec_drift` and `spec_registry` MCP tools exposing publish drift and ledger metadata)
- **Protocol Stages Executed**:
  - Stage 1: Context Scout (`review-context-scout`) — mapped changed files, callers, invariants, claims, and tests.
  - Stage 2: Parallel Risk Hunting (`review-risk-hunter` x 2) — concurrent correctness and security lanes, neuroslop pass on all assertions, and anti-parasitic architecture gate.
  - Stage 3: Adversarial Verification (`review-finding-verifier`) — challenged candidates, boundary conditions, concurrency, and suspicion map entries against snapshot source and tests.
  - Stage 4: Orchestrator Synthesis (`reviewer-kit`) — local synthesis of verified findings, coverage, and final verdict.
- **Active Skills Used**:
  - `skill://multi-stage-review`
  - `skill://reality-first-review`
- **Deterministic Suspicion Map Addressed**:
  - `tests/service/publish.test.mjs: assert lines +62/-0 (net +62)`: Inspected all 62 added assertions across 13 integration test scenarios in `tests/service/publish.test.mjs`. Verified that all assertions exercise real temporary bare git repositories, git clones, and live HTTP MCP services. Assertions verify annotated tag signatures, bot attestation tagger email, tree digest equality, draft skipping, version immutability, squatting refusal, versioned reads against tagged snapshots vs moved HEAD, 404 on unversioned specs, mutex concurrency serialization, path traversal rejection, binary blob/directory rejection, ref-unsafe version handling, remote tag adoption, leftover tag push recovery, and divergence refusal. None are vacuous, tautological, or mock echoes.

### Confirmed findings
None.

### Unproven/rejected summary
- `suspicion-publish-asserts`: Challenged `tests/service/publish.test.mjs` (+62 assertions) for vacuousness or tautological checks. Rejected because all 13 scenarios execute in live isolated git environments asserting positive and negative controls against real git objects and MCP HTTP endpoints.
- `boundary-ref-safety`: Challenged `isRefSafeVersion()` in `src/service/publish.js` for git refname injection vulnerabilities. Rejected because string length is bounded to 128 chars, characters are strictly whitelisted to alphanumeric start with `[A-Za-z0-9._/-]`, traversal segments (`..`), trailing dots/slashes, and git revision operators are explicitly forbidden, and tag refs are prefixed with `spec/${projectId}/${slug}/`.
- `boundary-path-containment`: Challenged `isContainedPathFragment()` and versioned document reads in `src/service/versioned.js` for path traversal or arbitrary blob reads. Rejected because containment checks forbid null bytes, leading slashes, backslashes, drive letters, empty segments, and `..`. Reads verify tree digest against `ledgerRow.digest`, verify git object type is `blob` (rejecting directories), and reject binary content containing null bytes.
- `ledger-concurrency-collision`: Challenged publish ledger concurrency and duplicate tag collisions. Rejected because concurrency is serialized in-memory per spec via Promise mutex queue, backed by atomic database uniqueness (`ON CONFLICT(spec_key, version) DO NOTHING`) in `SqliteStore` returning `changes: 0` to trigger digest comparison and collision detection.
- `divergence-handling`: Challenged handling of divergent remote tags in `src/service/publish.js`. Verified that when `remoteTagCommit !== null && remoteTagCommit !== ledgerRow.commitSha`, the publisher detects tag divergence, rejects publication with `VERSION_EXISTS`, logs `publish-rejected:VERSION_EXISTS` to `access_log`, and records the rejection event for `spec_drift` reporting.

### Notes
- None.

### Verified-OK
- Annotated tag creation and verification in `src/service/publish.js` with bot attestation signature and `tagger` email (`omp-spec-registry-bot@spec-kit.omp`).
- Versioned document retrieval in `src/service/versioned.js` pinned strictly to immutable ledger snapshot tree digest.
- Ref-safety check in `src/service/publish.js` (`isRefSafeVersion`) blocking malicious git tag ref manipulation.
- Path traversal defenses in `src/service/versioned.js` (`isContainedPathFragment`) rejecting directory traversal, backslashes, drive prefixes, and non-blob objects.
- Ledger deduplication and conflict safety in `src/service/ledger.js` under concurrent writes.
- Comprehensive integration suite in `tests/service/publish.test.mjs` covering all 13 positive and negative failure modes.

REVIEW_RESULT=PASS
