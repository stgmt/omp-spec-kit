# OMP Review Kit commit review

- staged diff hash: 9913d8d93a05629cccf8739714d9a977c9226b4a8049c08c2006281309f411d2
- result: PASS
- reviewer models tried: @smol

### Review coverage
- **Staged snapshot directory**: `C:\Users\stigm\AppData\Local\Temp\reviewer-kit-snapshot-vdWryG`
- **Diff hash**: `9913d8d93a05629cccf8739714d9a977c9226b4a8049c08c2006281309f411d2`
- **Changed files**:
  - `package.json`
  - `src/service/http.js`
  - `src/service/index.js`
  - `src/service/onboarding.js`
  - `tests/service/onboarding.test.mjs`
  - `tests/unit/service/onboarding.test.mjs`
- **Active review skills**: `skill://reality-first-review`, `skill://multi-stage-review`, `skill://developer-rules`, `skill://parasitic-architecture-audit`
- **Stages executed**:
  1. *Stage 1 (Context Scout)*: Identified change goal (TASK-8 automated onboarding credential issuance via YouTrack Hub API, scoped agent token minting, token revocation hygiene, `.mcp.json` snippet generation), callers (`http.js`, `index.js`, `ledger.js`, `auth.js`), test evidence (`test:unit` and `test:service` suites), and core invariants.
  2. *Stage 2 (Parallel Risk Hunting)*: Two parallel risk hunters (`lane: "correctness"`, `lane: "security"`) evaluated failure paths, boundary conditions, input sanitization, SSRF vectors, token hygiene, secret leakage, and anti-parasitic boundaries. 0 candidate defects generated.
  3. *Stage 3 (Adversarial Verification)*: Adversarial verifier inspected defenses across snapshot files, upstream YouTrack Hub interactions, timeouts, secret containment, and test suites. 0 confirmed defects.
  4. *Stage 4 (Orchestrator Synthesis)*: Local synthesis confirming zero P1/P2 defects and verified sound implementation.

### Confirmed findings
None.

### Unproven/rejected summary
None. No candidates were raised during Stage 2 risk hunting or Stage 3 verification.

### Verified-OK
- **Authentication & Authorization (`src/service/http.js:68-70`, `src/service/onboarding.js:148-154`)**: `POST /onboarding/token` is explicitly wrapped in `authGate`. The caller identity is derived strictly from `req.ctx.identity.login` (authenticated via YouTrack bearer token or HMAC-verified appBridge token). The caller cannot specify an arbitrary target login or elevate roles/scopes.
- **SSRF & Input Safety (`src/service/onboarding.js:175-178`, `src/service/onboarding.js:180-210`)**: `req.body.serviceUrl` is validated and only used as a string interpolation target for client response snippet URLs; it is never requested or contacted by the backend. All upstream Hub requests target `config.auth.youtrack.baseUrl` directly.
- **Fail-Closed Upstream Error Handling (`src/service/onboarding.js:31-72`, `src/service/onboarding.js:180-270`)**: Upstream fetch calls use `AbortSignal.timeout(UPSTREAM_TIMEOUT_MS)`. Errors from YouTrack Hub are classified into standard `OnboardingError` codes (`RETRYABLE_UPSTREAM`, `USER_NOT_FOUND`, `BANNED_USER`, `FORBIDDEN`, etc.) and mapped to appropriate HTTP status codes (503, 404, 403) without leaking internal traces or tokens.
- **Credential Hygiene & Secret Containment (`src/service/onboarding.js:255-267`, `tests/service/onboarding.test.mjs:125-140`)**: The minted token is returned only in the response body to the authenticated caller. The audit log records solely `{ op: 'onboarding', login, role, tenant, result: 'ok:token-issued:revoked-N' }`. Neither the service token nor the user permanent token is logged to stdout or stored in the database access log.
- **Token Accumulation Mitigation (`src/service/onboarding.js:235-248`)**: Existing tokens matching `ONBOARDING_TOKEN_NAME` ('omp-spec-kit agent') for that user are cleanly revoked via Hub DELETE before or alongside new token issuance.
- **Test Coverage (`tests/unit/service/onboarding.test.mjs`, `tests/service/onboarding.test.mjs`)**: Thorough unit tests covering user lookup, multi-service scoping, token revocation, schema conformance, and fail-closed error handling; integration tests prove the minted token authenticates against `/mcp`, lists tools, and executes spec operations against live YouTrack fixtures.

REVIEW_RESULT=PASS

Working...
