# OMP Review Kit commit review

- staged diff hash: b0aa69eb1cba1f73c815cbae22eb91ca4d51f76c643ddf90b181b9d642469414
- result: BLOCK
- reviewer models tried: @smol

## Normalized rejection envelope

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "review_failure",
  "diff_hash": "b0aa69eb1cba1f73c815cbae22eb91ca4d51f76c643ddf90b181b9d642469414",
  "findings": [],
  "failure": {
    "code": "malformed_rejection_envelope",
    "message": "The rejection envelope was malformed or violated its schema."
  }
}
```

### Verified-OK
- The staged index was materialized into a temporary snapshot before review.
- The reviewer ran from the repository root, preserving Git and project context.
- The reviewer process exited successfully and its verdict was normalized.

REVIEW_REJECTION_ENVELOPE_BEGIN
{
  "schema": "review-rejection-envelope@1",
  "kind": "confirmed_findings",
  "diff_hash": "b0aa69eb1cba1f73c815cbae22eb91ca4d51f76c643ddf90b181b9d642469414",
  "findings": [
    {
      "finding_id": "correctness-1",
      "priority": "P1",
      "defect_class": "correctness",
      "file_path": "scripts/check-spec-corpus.mjs",
      "line_start": 73,
      "line_end": 75,
      "counterexample": "Run `npm run check:spec-corpus`, `npm run verify`, or `npm test` against the staged commit.",
      "verifier_argument": "The staged commit deletes all 98 specification files under .specs/ from git tracking, but omits staging the corresponding changes to package.json and scripts/check-spec-corpus.mjs. In scripts/check-spec-corpus.mjs:73, exactCanonicalDocuments executes fs.readdirSync(SPECS_ROOT) where SPECS_ROOT is path.join(ROOT, '.specs'). Because .specs was deleted, Node crashes with unhandled ENOENT: no such file or directory, scandir '...\\.specs', exiting with status 1 and breaking npm run verify, npm test, and GitHub Actions CI."
    }
  ]
}
REVIEW_REJECTION_ENVELOPE_END

### Review coverage
- **Inspected diff**: 115 files touched in the staged index (`b0aa69eb1cba1f73c815cbae22eb91ca4d51f76c643ddf90b181b9d642469414`).
  - Staged deletion of 98 specification files under `.specs/*` across 7 specifications (`agent-ux-elicitation-guard`, `plugin-distribution`, `roadmap-roadmaps`, `spec-mcp-access-gate`, `spec-mcp-operations`, `spec-registry-service`, `youtrack-visualization`).
  - Staged test/probe updates aligning schema version assertions from `'spec-kernel@1'` to `KERNEL_SCHEMA_VERSION` (`'spec-kernel@2'`):
    - `scripts/probe-omp-discovery-v18.0.11.mjs`
    - `tests/helpers/evidence-e2e.mjs`
    - `tests/helpers/tool-e2e.mjs`
    - `tests/step-definitions/mcp-release-integrity.steps.mjs`
    - `tests/step-definitions/release-candidate.steps.mjs`
    - `tests/step-definitions/safe-authoring.steps.mjs`
    - `tests/step-definitions/spec-kernel.steps.mjs`
    - `tests/step-definitions/staged-mcp.steps.mjs`
    - `tests/validation-observable-matrix.test.mjs`
- **Active review skills**: `skill://reality-first-review`, `skill://multi-stage-review`, `skill://delete-only-subsystem-cutover`.
- **Stages executed**:
  - Stage 1: Context Scout (mapped goals, invariants, callers, test suites, and schema version contracts).
  - Stage 2: Parallel Risk Hunting (Correctness lane explored schema migration and corpus deletion side effects; Security lane investigated sinks, inputs, and traversals).
  - Stage 3: Adversarial Verification (reproduced crash in snapshot, proved trigger reachability and diff introduction).
  - Stage 4: Orchestrator Synthesis.

### Confirmed findings
- **Priority**: P1
  **File path**: `scripts/check-spec-corpus.mjs` (introduced by staged deletion of `.specs/*` without accompanying script/manifest synchronization)
  **Line range**: 73-75
  **Observed behavior**: The staged index deletes all 98 files in `.specs/` (the entire `.specs/` tree) from git tracking, but does not stage the necessary updates to `scripts/check-spec-corpus.mjs` and `package.json`. When `scripts/check-spec-corpus.mjs` runs, `exactCanonicalDocuments` unconditionally executes `fs.readdirSync(SPECS_ROOT, { withFileTypes: true })` on `.specs/`. Since `.specs/` does not exist in the snapshot, Node throws an unhandled `Error: ENOENT: no such file or directory, scandir '...\\.specs'` and crashes with exit code 1.
  **Expected behavior**: When `.specs/` is deleted from git tracking, the accompanying scripts (`scripts/check-spec-corpus.mjs` and `package.json`) must be staged in the same commit (e.g. replacing `npm run check:spec-corpus` with `npm run check:no-root-specs`, or guarding `fs.existsSync(SPECS_ROOT)`), ensuring `npm run verify`, `npm test`, and CI remain functional.
  **Trigger scenario**: Run `npm run check:spec-corpus`, `npm run verify`, or `npm test` on the staged commit.
  **Impact**: Deterministic build/CI failure. `npm run verify` and `npm test` immediately fail with exit code 1 due to the unhandled ENOENT crash in `check-spec-corpus.mjs`.
  **Repository evidence**:
  - `package.json:11` defines `"verify": "node scripts/verify-marketplace.mjs && node scripts/verify-package.mjs && npm run check:spec-corpus && npm run check:spec-port && npm run check:blast && npm run check:mutations"`
  - `scripts/check-spec-corpus.mjs:73` executes `fs.readdirSync(SPECS_ROOT, { withFileTypes: true })`
  - In snapshot `C:\Users\stigm\AppData\Local\Temp\reviewer-kit-snapshot-2DCS4g`, running `node scripts/check-spec-corpus.mjs` fails with exit status 1: `ENOENT: no such file or directory, scandir '...\\.specs'`
  - Staging the unstaged changes to `package.json` and `scripts/check-spec-corpus.mjs` resolves this issue.

### Unproven/rejected summary
- **Security Lane**: Checked for dangerous sinks, input validation bypasses, permission escalations, or path traversal vectors in the updated test step definitions and discovery probe. None found; changes in test files strictly update schema assertions to `'spec-kernel@2'`.
- **Schema Migration (`spec-kernel@1` -> `spec-kernel@2`)**: Staged updates to test assertions and helper files consistently adapt to the new kernel version constant. No P1/P2 regression introduced within the modified test helpers themselves.

REVIEW_RESULT=BLOCK

Working...
