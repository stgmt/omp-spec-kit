# OMP Review Kit commit review

- staged diff hash: 812005acf6199b6251061619301251fe33b023a9582fe800e1fa54396ebadc5d
- result: BLOCK
- reviewer models tried: @smol

## Normalized rejection envelope

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "review_failure",
  "diff_hash": "812005acf6199b6251061619301251fe33b023a9582fe800e1fa54396ebadc5d",
  "findings": [],
  "failure": {
    "code": "malformed_rejection_envelope",
    "message": "The rejection envelope was malformed or violated its schema."
  }
}
```

### Verified-OK
- The staged index was materialized into a temporary snapshot before review.
- The reviewer ran from the repository root, preserving Git and project context.
- The reviewer process exited successfully and its verdict was normalized.

```json
{
  "schema": "review-rejection-envelope@1",
  "kind": "confirmed_findings",
  "diff_hash": "812005acf6199b6251061619301251fe33b023a9582fe800e1fa54396ebadc5d",
  "findings": [
    {
      "finding_id": "correctness-1",
      "priority": "P1",
      "defect_class": "correctness",
      "file_path": "src/service/publish.js",
      "line_start": 75,
      "line_end": 124,
      "verifier_argument": "lsRemote returns tag object SHA at refs/tags/<tag> and peeled commit SHA at refs/tags/<tag>^{}. In publish.js:76, remote.get(tagRef) !== ledgerRow.commitSha compares tag object SHA to commit SHA, which is always true for annotated tags. This causes git.push to run redundantly on every publish/sync pass (line 85), push-retry conflict check to always fail (line 99), and tag adoption to write the unpeeled tag object SHA to the database ledger commitSha (line 113).",
      "counterexample": "Publish an ACTIVE spec, then invoke publishSpec again or trigger periodic sync; remote.get(tagRef) !== ledgerRow.commitSha evaluates to true because tag object SHA !== commit SHA, invoking git push to remote origin on every pass. In tag adoption, insertLedgerIfAbsent records the tag object SHA in the database, corrupting subsequent spec_documents reads."
    },
    {
      "finding_id": "security-1",
      "priority": "P1",
      "defect_class": "security",
      "file_path": "src/service/versioned.js",
      "line_start": 79,
      "line_end": 88,
      "verifier_argument": "versioned.js:79-81 constructs docPath as `${specPath}/${args.doc}` and executes git.show(row.commitSha, docPath) without path containment or traversal sanitization. When args.doc contains '..', Git interprets it as a revision with a pathspec filter and outputs commit metadata and unified diffs of files across the repository matching the pathspec, disclosing arbitrary repository files and commit logs across tenant/project boundaries.",
      "counterexample": "An authenticated reader calls spec_documents with action: 'read', spec: '<spec>', version: '<version>', and doc: '../../../../package.json'. git.show executes with the traversed pathspec and returns commit log and diff content across the repository in a successful kind: 'document' envelope instead of returning PATH_FORBIDDEN."
    },
    {
      "finding_id": "security-2",
      "priority": "P2",
      "defect_class": "security",
      "file_path": "src/service/publish.js",
      "line_start": 81,
      "line_end": 86,
      "verifier_argument": "authoredField in registry.js extracts Version without refname validation. publish.js line 55 only checks if (!snap.version) and passes it directly to git.tag at line 81 without error boundaries. If Version contains invalid Git ref characters (e.g. '1.0..0', spaces, or '1.0.0 [draft]'), git.tag throws GitError (exit 128), which is unhandled and causes publishProject and publishAll loops in publish.js lines 151 and 162 to terminate, blocking publication across all projects.",
      "counterexample": "A spec author sets 'Version: 1.0..0' in README.md of an ACTIVE spec. Service startup or background sync calls publishAll(), which calls publishSpec on that spec; git.tag fails with fatal exit code 128, raising an unhandled rejection that halts the publish loop and prevents all subsequent valid specs across the repository from publishing."
    }
  ]
}
```

REVIEW_REJECTION_ENVELOPE_BEGIN
{
  "schema": "review-rejection-envelope@1",
  "kind": "confirmed_findings",
  "diff_hash": "812005acf6199b6251061619301251fe33b023a9582fe800e1fa54396ebadc5d",
  "findings": [
    {
      "finding_id": "correctness-1",
      "priority": "P1",
      "defect_class": "correctness",
      "file_path": "src/service/publish.js",
      "line_start": 75,
      "line_end": 124,
      "verifier_argument": "lsRemote returns tag object SHA at refs/tags/<tag> and peeled commit SHA at refs/tags/<tag>^{}. In publish.js:76, remote.get(tagRef) !== ledgerRow.commitSha compares tag object SHA to commit SHA, which is always true for annotated tags. This causes git.push to run redundantly on every publish/sync pass (line 85), push-retry conflict check to always fail (line 99), and tag adoption to write the unpeeled tag object SHA to the database ledger commitSha (line 113).",
      "counterexample": "Publish an ACTIVE spec, then invoke publishSpec again or trigger periodic sync; remote.get(tagRef) !== ledgerRow.commitSha evaluates to true because tag object SHA !== commit SHA, invoking git push to remote origin on every pass. In tag adoption, insertLedgerIfAbsent records the tag object SHA in the database, corrupting subsequent spec_documents reads."
    },
    {
      "finding_id": "security-1",
      "priority": "P1",
      "defect_class": "security",
      "file_path": "src/service/versioned.js",
      "line_start": 79,
      "line_end": 88,
      "verifier_argument": "versioned.js:79-81 constructs docPath as `${specPath}/${args.doc}` and executes git.show(row.commitSha, docPath) without path containment or traversal sanitization. When args.doc contains '..', Git interprets it as a revision with a pathspec filter and outputs commit metadata and unified diffs of files across the repository matching the pathspec, disclosing arbitrary repository files and commit logs across tenant/project boundaries.",
      "counterexample": "An authenticated reader calls spec_documents with action: 'read', spec: '<spec>', version: '<version>', and doc: '../../../../package.json'. git.show executes with the traversed pathspec and returns commit log and diff content across the repository in a successful kind: 'document' envelope instead of returning PATH_FORBIDDEN."
    },
    {
      "finding_id": "security-2",
      "priority": "P2",
      "defect_class": "security",
      "file_path": "src/service/publish.js",
      "line_start": 81,
      "line_end": 86,
      "verifier_argument": "authoredField in registry.js extracts Version without refname validation. publish.js line 55 only checks if (!snap.version) and passes it directly to git.tag at line 81 without error boundaries. If Version contains invalid Git ref characters (e.g. '1.0..0', spaces, or '1.0.0 [draft]'), git.tag throws GitError (exit 128), which is unhandled and causes publishProject and publishAll loops in publish.js lines 151 and 162 to terminate, blocking publication across all projects.",
      "counterexample": "A spec author sets 'Version: 1.0..0' in README.md of an ACTIVE spec. Service startup or background sync calls publishAll(), which calls publishSpec on that spec; git.tag fails with fatal exit code 128, raising an unhandled rejection that halts the publish loop and prevents all subsequent valid specs across the repository from publishing."
    }
  ]
}
REVIEW_REJECTION_ENVELOPE_END

REVIEW_RESULT=BLOCK
