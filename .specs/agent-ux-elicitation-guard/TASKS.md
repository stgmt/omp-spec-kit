# Tasks

Implementation and verification tasks for the elicitation guard. All tasks below are complete; the linked acceptance scenarios and gates are the evidence record.

## TASK-1: Write the elicitation memo

**Status:** done
Done when: the canonical memo exists with trigger, source plus gap plus question steps, dumb-question ban, and do-it-now mode.

## TASK-2: Add the shared hint constant and wire references

**Status:** done
Done when: one hint constant is exported from the contract module and server instructions plus the stop error reference it with no full-text copy, while the raw-write enforcement hook carries no elicitation text.

## TASK-3: Implement the first-write stop

**Status:** done
Done when: a real apply for a missing canonical Markdown document is refused once with ELICITATION_REQUIRED and zero bytes changed.

## TASK-4: Implement error content and ticket consumption

**Status:** done
Done when: the error carries memo path, target, and retry rule, and the next valid retry commits while consuming the ticket.

## TASK-5: Exempt later writes

**Status:** done
Done when: retries, changed retries, and all later edits pass without a second stop; a fresh service may issue one stop for a still-missing target, and its retry passes.

## TASK-6: Keep existing corpus untouched and list the new spec

**Status:** done
Done when: pre-existing documents never meet the stop and the corpus gate lists the new specification with a green check.

## TASK-7: Prove the replay scenarios

**Status:** done
Done when: first refusal, zero-byte preservation, retry success, later-edit success, multi-document behavior, preview exemption, package discovery, and the live OMP manager path pass without private-state assertions.

## TASK-8: Rebuild the distributed plugin

**Status:** done
Done when: the build script regenerates the distribution from sources with the memo included and no hand edit to generated output.

## TASK-9: Close inspection plus corpus plus suites

**Status:** done
Done when: spec_inspect validation, link sweep, corpus gate, and the affected scenario suites are green with no weakened assertion.

## TASK-10: Record the release note

**Status:** done
Done when: the changelog entry names the guard, the one-time stop, the memo path, and the retry rule.
