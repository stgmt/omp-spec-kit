{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Spec Registry Service",
  "description": "Connection to spec-registryd (TASK-12 auth path). The bridge token must match auth.appBridge.token in the service config.",
  "properties": {
    "serviceUrl": {
      "title": "Service URL",
      "description": "Base URL of spec-registryd, e.g. http://spec-registryd:8642 or https://specs.example.com",
      "type": "string",
      "minLength": 1,
      "x-scope": "GLOBAL"
    },
    "serviceBridgeToken": {
      "title": "Service bridge token",
      "description": "Secret shared with the service (auth.appBridge.token)",
      "type": "string",
      "format": "secret",
      "minLength": 16,
      "x-scope": "GLOBAL"
    }
  },
  "required": ["serviceUrl", "serviceBridgeToken"]
}
